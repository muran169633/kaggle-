# v9(2) 说明

本目录当前 README 仅介绍 `v9(2)` 版本实验。

## 文件用途
`v9(2)` 用于 NFL 任务的第 9 版实验迭代，主要记录该版本的数据处理、特征实验、模型训练与结果验证流程。

## 重点内容
1. 数据预处理与样本构建。
2. 模型结构与训练参数调整。
3. 验证集指标与版本对比结论。

## 使用方式
1. 打开 `v9(2)` 对应 Notebook。
2. 按单元顺序依次运行。
3. 根据输出结果记录指标并保存关键产物。

## 备注
该 README 不再介绍其他版本文件。
